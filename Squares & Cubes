#include <stdio.h>
int main()
{
    int a, square, cube;
    printf("Enter an integer: ");
    scanf("%i", &a);
    square = a * a;
    cube = a * a * a;
    printf("Square of %d is: %i \n", a, square);
    printf("Cube of %d is: %i", a, cube);    
}
